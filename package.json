{
	"name": "logpulse-monorepo",
	"version": "0.0.0",
	"private": true,
	"workspaces": ["packages/*"],
	"catalog": {
		"typescript": "^5.7.0",
		"@types/node": "^22.0.0",
		"react": "^19.0.0",
		"react-dom": "^19.0.0",
		"@types/react": "^19.0.0",
		"@types/react-dom": "^19.0.0"
	},
	"scripts": {
		"build": "bun run --filter '*' build",
		"test": "bun run --filter '*' test",
		"lint": "biome check .",
		"format": "biome format --write .",
		"typecheck": "bun run --filter '*' typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"typescript": "catalog:"
	}
}
